package com.miracle.smart_ecommerce_jpa.security;

// JWT token provider removed when Spring Security and JWT were disabled.
// This class remains as a non-bean utility placeholder to avoid compile errors in other modules.
public final class JwtTokenProvider {
    private JwtTokenProvider() {}

    // Token functionality intentionally disabled. If you need tokens again, restore implementation.
}
